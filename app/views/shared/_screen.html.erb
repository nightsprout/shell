  <div
    id="ns-screen-background"
    ng-if="nsStateMachine.modal.isVisible || nsStateMachine.warningPopup.isVisible"
    ng-click="nsStateMachine.modal.close()"
    >
  </div>
  <!-- #ns-warning-background -->